<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/scripts/login.js"></script>
    <script src="/scripts/product.js"></script>
    <title>Women's Shoes</title>
    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/shoes-page.css">
</head>
<body>
    <div id="page-container">
        <header>
            <%- include('partials/header') %>
        </header>

        <main>
            <h1>Women's Shoes</h1>
            <section class="featured-products">
                <h2>All Women's Shoes</h2>
                <div class="products">
                    <% 
                    // Filter shoes for women's category and stock > 0
                    let womenShoes = shoes.filter(shoe => shoe.category === "Women" && shoe.stock > 0);

                    // Loop to display all women's shoes with stock greater than 0
                    for (let i = 0; i < womenShoes.length; i++) {
                    %>
                        <div class="product" data-id="<%= womenShoes[i]._id %>">
                            <img src="<%= womenShoes[i].image %>" alt="<%= womenShoes[i].title %>">
                            <h3 class="title"><%= womenShoes[i].title %></h3>
                            <p class="price">$<%= womenShoes[i].price %></p>
                        </div>
                    <% 
                    }
                    %>
                </div>
            </section>
        </main>

        <footer id="mainFooter">
            <%- include('partials/footer') %>
        </footer>
    </div>

    <script>
    $(document).ready(function() {
        console.log('Document ready');

        $('.product').click(function(e) {
            e.preventDefault();
            const productId = $(this).data('id');

            $.ajax({
                url: `/shoes/${productId}/ajax`,
                method: 'GET',
                success: function(response) {
                    $('main').html(response);
                },
                error: function(err) {
                    console.error('Error loading the product details:', err);
                }
            });
        });
    });
    </script>
</body>
</html>